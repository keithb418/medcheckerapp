define(function (require) {
  var App = require('app');

  return Backbone.Marionette.Controller.extend({

    showAlert: function (opts) {

    },
    showSessionWarning: function (opts) {

    }
  });
});
